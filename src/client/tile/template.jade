.tile(ng-style="style()")
    ._inner(
        ng-class="{'-reduced': tile.reduced}"
        class="-level-{{tile.level}}"
    )
        div(ng-class="powerup()")
        div(ng-class="meta()")
        ._value {{tile.value}}